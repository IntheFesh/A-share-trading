"""Phase 2:事件级回测(唯一真值)+ 成本六层 + 基线 + 指标 + 压力。

对应 v3.1 第四章成本、第十章风控、第十一章引擎与出场状态机。
``engine.py`` 是全系统**唯一真值**:T+1/涨跌停/出场状态机都在此实现,且 INV-2 守卫(执行用 raw)、
INV-3 同源(成交判定函数 is_tradeable_fill 的权威实现在此,labels/ 从此 import)落地于此。
"""
